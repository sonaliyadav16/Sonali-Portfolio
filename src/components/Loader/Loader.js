import React from "react";
import LoaderContainer from "../../containers/LoaderContainer";


const Loader = () => {
    return (
        <LoaderContainer>
            
        </LoaderContainer>
    );
};

export default Loader;
